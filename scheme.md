# scheme.md — bearing-diagnostics 判断ログ

形式: Built / Observed / Decision / Consequence(ステージ単位で追記)

## C0 準備(2026-07-26)

- Built: リポジトリ骨格(src/lib, docs/figs, data, tests)、venv。
- Decision: **CWRU(正解既知)で手法を固定してから IMS(run-to-failure)に適用する二段構え。**
  geo-change-detection B4 の「選択を測定可能な時点まで遅らせる」の応用。正解のない
  データに対して手法を選ぶと、選択が評価に混入して検証できなくなる。
- Decision: 生データは `data/` に置き gitignore。出典 URL・ファイル名・サイズ・
  チェックサムは `docs/C0-observation.md` に記録し、再現可能性を担保する。
- Decision: 故障特徴周波数(BPFO/BPFI/BSF/FTF)は**データを見る前に**軸受幾何と
  回転数から計算して宣言する。スペクトルを見てから「合う周波数」を探すと、
  B5 で学んだ「調整した自由度」が増えて一致の情報量が消えるため。
- Observed: 本人が 6205-2RS の特徴周波数を幾何から手計算する過程で、参照していた
  市販教材(振動診断の技術講座資料)の式に**誤植2件**を発見した。①BPFI の
  「1 + (d/D)cosα」の + が「・」に化けている ②BSF の係数が fr(軸回転)ではなく
  fo(外輪通過周波数)になっている。本人の途中の誤答 8.448 は後者の式を忠実に
  適用した値と一致し、出所まで特定できた。検出に効いたのは sanity check 3本:
  BPFI > BPFO / BSF < D/2d / 玉の自転周波数は玉数 Z に依存し得ない。
- Consequence: ライブラリだけでなく**教科書・公式集も突き合わせの対象**。
  一致・不一致の判定には式の再導出と物理的上下界が要る(A/B 系の型の拡張)。

## C0 完了(2026-07-27)

- Built: `docs/C0-observation.md`(出典検分・幾何・宣言値・定着チェック)。データDL完了
  (CWRU 4ファイル、IMS 1.06GB 展開済み)。
- Observed: **IMS の Readme とデータ実体の食い違い3件** — ①fs 表記(20kHz vs 20,480点/1s)
  ②Set3 実体は `4th_test` フォルダ ③Set3 は 6,324ファイル・04/18 まで(公称 4,448・04/04)。
  公開ベンチマークでもメタデータは検分してから使う。
- Observed: 教材誤植式(BSF の fo 基準)に本人が**2回**引っかかった(8.448 と 992 Hz、完全同型)。
  誤植の発見と正式の定着は別。上界検算 BSF < (D/2d)fr が2回とも検出した。
- Decision: fs = 20.48 kHz 採用(±2.4% は系統誤差として照合窓と別勘定。全線が
  ×1.024 で一斉にずれる離散仮説として C4 で識別可能)。
- Decision: C4 主対象 = Set2(公称完全一致・外輪故障)。
- 定着チェック: Q1 は式○・物理は「和が 2Z になる」自己矛盾を検算則で修正して通過。
  Q2 ○(系統誤差の性質を補足)。Q3 は「教師なし学習」(手法名)から再質問を経て
  「宣言済み 236.4 Hz 付近のピーク成長が外輪ラベルと整合することを証拠にする」で通過。
- Consequence: C5 の評価は ①周波数指紋とラベルの整合 ②検出が故障より先行 ③初期健全区間
  (仮定として宣言)での誤報率、の3本柱で組む。劣化開始の正解が無いことを隠さない。

## C1 進行中(2026-07-27)

- Built: `src/lib/indicators.py`(rms / kurtosis / crest_factor、平均除去・Pearson・1/N を宣言)、
  `src/c1_compare.py`(scipy/numpy 照合)。
- Observed: 規約明示(fisher=False, bias=True)で3指標×3信号すべて rel=0 の厳密一致。
  scipy 既定値との差は3信号とも**厳密に 3.000**(Fisher/Pearson の定義差の指紋)。
  bias=False との差は rel ≈ 1e-4 ≈ c/N(c≈2)。
- Consequence(一般知見・C6候補): **乖離は3スケールに分離する** — 定義差 O(1) /
  補正差 O(1/N) / 丸め ~1e-16。照合閾値は「小さく」ではなく**分離したい2スケールの
  空隙に置く**(rtol=1e-12)。これにより規約取り違えを必ず検出し丸めを誤検出しない。
- コード読解: kurtosis(DC混入で尖度→1 / m2=0 で numpy は nan を黙って返す /
  手トレース 3.25 一致)、rms(=母集団標準偏差)、crest_factor 完了。
- Decision(予測宣言・図を描く前): **「尖度が先・RMS が後」の教科書ストーリーは
  Set2 ch1 では綺麗に現れない**と本人が予測。根拠: 初日から尖度 3.63・CF 6.32 と
  健全期にしてはやや衝撃的。ループ4のトレンド図で検証する。
- Observed(ループ4・実測): 予測は**的中したが外れ方が想定と違った** — RMS(3.70日)が
  尖度(4.79日)に**丸1日先行**し、教科書と順序が逆。CF は+5σを一度も超えず。
  一因は RMS ベースラインの静けさ(CV 1.4%)。**感度=物理×ベースライン変動**。
- Observed: B3 は全期間もっとも衝撃的(尖度4〜6, CF 7〜10)だが壊れていない。
  「尖度が高い=異常」の単純警報は不成立。5.3日に healing(剥離エッジ平滑化)による
  一時回復。最終2点は機械停止後の録音(4ch同時に rms≈0.001)。
- Decision: **床規則 rms < 0.01 g で機械停止を自動除外**(C4/C5 に引き継ぎ)。
  立ち上がり判定は「他chにもあるか」でなく「**同時か遅れて追随か**」で切る
  (同時=環境、遅れ=B1からの伝播)。B1 の 3.70日検出は健全chが1.16日以上
  遅れることから支持。
- 仮説(未検証・C2で部分検証): RMS先行の機構は潤滑膜切れ→摩擦増大の広帯域上昇。
- feedback: トレンド図の端ラベル重なりを本人指摘で修正(凡例追加+ピクセル単位の
  重なり回避)。図は読まれて初めて成果物。

## C2 進行中(2026-07-27)

- Built: `lib/spectral.py`(dft_naive / fft_radix2 / hann / welch_psd)、`c2_compare.py`、
  `c2_spectra.py`。Nseg=2048 は本人が主葉幅×BPFO/BPFI間隔から導出(1354→切り上げ)。
- Observed: 照合は全て事前宣言どおり(welch↔scipy ~1e-15)。dft_naive の丸めは FFT より
  3桁大きい(演算量削減=丸め削減)。リーケージ実測: 0.4bin ずれで峰binに57%しか残らない
  (エージェントの「漏れない」出題を本人が違和感指摘→定量で訂正。問題文も検証対象)。
- Observed(スペクトル3時点): 賭けB(離散線が既に立つ)勝利 — 236Hz間隔の櫛が day3.8 で
  ×3〜×10。潤滑仮説は主因棄却。側帯無し(C0幾何予言的中)。**櫛間隔236Hz→fs=20.48kHz確定・
  すべり0.17%**(C0の離散仮説判別が機能)。
- Observed(237Hz線): Δf=10Hzでは240.0に見えた(bin量子化の罠→Δf=1Hzで測り直し237Hz)。
  本人が判定表を先に書き4ch強度勾配で**B3起源の伝播と確定**(ch3がch1の14倍)。
  C1の「B3は衝撃的だが壊れない」の正体の一部。低周波は遠達/高周波は局所 →
  **C3で高周波共振帯にエンベロープを掛ける根拠は空間選択性でもある**。
- Decision: ピーク位置の主張はΔfを明記。線の振幅は±数bin積分で拾う(C4規則)。

## C4 (2026-07-27)

- Built: `c4_trend.py` — 回顧帯域(3–6.5kHz, lookahead明記)と因果帯域(オンライン
  Kurtogram+中央値5)の2本立て。lookahead bias をトレードのバックテスト同型として
  本人が翻訳。
- Observed: 立ち上がり RMS 3.701 / エンベロープ回顧 3.694 / 因果 3.743 / 尖度 4.792 —
  **エンベロープはRMSと事実上同着**。賭け(2.8-3.5日)は外れ: 分子の物理ゲインを
  分母σの高CV(21% vs RMSの1.4%)が相殺(=「感度は物理×ベースラインの静けさ」の再演)。
- Observed: 予測された「237Hzの壁」は不発 — B3の237Hzは**加算**的低周波線であり
  帯域の**変調**ではないため、帯域通過で消えた。外れ方が加算vs変調の区別を確定。
- Observed(予期しない発見): 窓内ピーク位置が3.69日を境に**236Hzへ完全ロック**
  (4.5-6.5日で287/288点)。本人が **FLI(周波数ロック指標)** を第3の検出器として提案。
- Decision: C5の検出器メニュー = RMS/尖度/線床比(回顧・因果)/FLI。lookahead有無を
  検出器ごとに明記。リードタイムは間欠観測ぶん保守的。

## C3 進行中(2026-07-27)

- Built: `lib/envelope.py`(analytic/bandpass/envelope_spectrum)、`c3_compare.py`、`c3_cwru.py`。
  賭けシート(3欠陥の主線+側帯構造)を本人がエンベロープ実行前に宣言。帯域選定は
  PSD比最大の1kHz窓という手続きに固定(自由度の事前封鎖)。
- Observed: analytic↔scipy ~5e-16(奇数N含む)。合成AM: エンベロープ線/床431、
  生スペクトル1.32 — 「情報は包絡線」の実証。
- Observed(CWRU known-answer): 内輪=完勝(−0.31%、±fr側帯あり=荷重域通過の物理が閉じた)。
  外輪=主線+0.20%・線/床105だが側帯が弱く存在(14%)→**側帯は二値でなく
  「回転同期動荷重の寄与率に比例する連続量」と本人が規則を書き直し**(IMS 0%/CWRU 14%/
  内輪36%の3点整合)。
- Observed(玉=賭け外れ→本人が切り分け実験を設計): 尖度×帯域グリッド(500Hz〜Nyquist、
  BSF族4本マッピング)で全帯域尖度≈3・線なし → **ラベルに対し振動シグネチャ不在(b)と確定**。
  Smith & Randall 2015 の「CWRUには診断不能記録がある」と整合。先に測ってから文献に
  当てた順序も含めて C6 の教訓: **ラベルはGround Truthの十分条件ではない**。

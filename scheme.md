# scheme.md — bearing-diagnostics 判断ログ

形式: Built / Observed / Decision / Consequence(ステージ単位で追記)

## C0 準備(2026-07-26)

- Built: リポジトリ骨格(src/lib, docs/figs, data, tests)、venv。
- Decision: **CWRU(正解既知)で手法を固定してから IMS(run-to-failure)に適用する二段構え。**
  geo-change-detection B4 の「選択を測定可能な時点まで遅らせる」の応用。正解のない
  データに対して手法を選ぶと、選択が評価に混入して検証できなくなる。
- Decision: 生データは `data/` に置き gitignore。出典 URL・ファイル名・サイズ・
  チェックサムは `docs/C0-observation.md` に記録し、再現可能性を担保する。
- Decision: 故障特徴周波数(BPFO/BPFI/BSF/FTF)は**データを見る前に**軸受幾何と
  回転数から計算して宣言する。スペクトルを見てから「合う周波数」を探すと、
  B5 で学んだ「調整した自由度」が増えて一致の情報量が消えるため。
- Observed: 本人が 6205-2RS の特徴周波数を幾何から手計算する過程で、参照していた
  市販教材(振動診断の技術講座資料)の式に**誤植2件**を発見した。①BPFI の
  「1 + (d/D)cosα」の + が「・」に化けている ②BSF の係数が fr(軸回転)ではなく
  fo(外輪通過周波数)になっている。本人の途中の誤答 8.448 は後者の式を忠実に
  適用した値と一致し、出所まで特定できた。検出に効いたのは sanity check 3本:
  BPFI > BPFO / BSF < D/2d / 玉の自転周波数は玉数 Z に依存し得ない。
- Consequence: ライブラリだけでなく**教科書・公式集も突き合わせの対象**。
  一致・不一致の判定には式の再導出と物理的上下界が要る(A/B 系の型の拡張)。

## C0 完了(2026-07-27)

- Built: `docs/C0-observation.md`(出典検分・幾何・宣言値・定着チェック)。データDL完了
  (CWRU 4ファイル、IMS 1.06GB 展開済み)。
- Observed: **IMS の Readme とデータ実体の食い違い3件** — ①fs 表記(20kHz vs 20,480点/1s)
  ②Set3 実体は `4th_test` フォルダ ③Set3 は 6,324ファイル・04/18 まで(公称 4,448・04/04)。
  公開ベンチマークでもメタデータは検分してから使う。
- Observed: 教材誤植式(BSF の fo 基準)に本人が**2回**引っかかった(8.448 と 992 Hz、完全同型)。
  誤植の発見と正式の定着は別。上界検算 BSF < (D/2d)fr が2回とも検出した。
- Decision: fs = 20.48 kHz 採用(±2.4% は系統誤差として照合窓と別勘定。全線が
  ×1.024 で一斉にずれる離散仮説として C4 で識別可能)。
- Decision: C4 主対象 = Set2(公称完全一致・外輪故障)。
- 定着チェック: Q1 は式○・物理は「和が 2Z になる」自己矛盾を検算則で修正して通過。
  Q2 ○(系統誤差の性質を補足)。Q3 は「教師なし学習」(手法名)から再質問を経て
  「宣言済み 236.4 Hz 付近のピーク成長が外輪ラベルと整合することを証拠にする」で通過。
- Consequence: C5 の評価は ①周波数指紋とラベルの整合 ②検出が故障より先行 ③初期健全区間
  (仮定として宣言)での誤報率、の3本柱で組む。劣化開始の正解が無いことを隠さない。
